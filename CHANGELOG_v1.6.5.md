# TKD Judge App v1.6.5

## Bug fix
- Fixed the JSX parse error in App.js caused by closing the dynamic AppRoot component with `</SafeAreaView>`.
- The joined scoring screen now correctly closes with `</AppRoot>`, allowing the Expo bundle to parse and launch.
- Version updated to 1.6.5 (Android versionCode 164, iOS build 164).

All v1.6.4 UI, scoring, swipe, sound/vibration, reconnect, settings, fullscreen, logo and font changes are retained.
